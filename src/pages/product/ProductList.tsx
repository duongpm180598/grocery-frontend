import { Card } from '@mui/material';
import ProductTable from './ProductTable';

const ProductList = () => {
  return (
    <Card>
      <ProductTable />
    </Card>
  );
};

export default ProductList;
